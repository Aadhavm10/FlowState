{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "vite",
  "rewrites": [
    { "source": "/api/library-proxy/:path*", "destination": "/api/library-proxy?path=:path*" },
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
